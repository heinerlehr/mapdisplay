[project]
name = "mapdisplay"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.128.5",
    "pandas>=3.0.0",
    "panel[fastapi]>=1.8.7",
    "pytest>=9.0.2",
    "xarray>=2026.1.0",
]
